include: package:yumemi_lints/flutter/3.35/recommended.yaml

analyzer:
  exclude:
    - build/**
    - .dart_tool/**
  errors:
    invalid_annotation_target: ignore
  plugins:
    - custom_lint

formatter:
  trailing_commas: preserve
